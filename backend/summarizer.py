def fetch_article_text(ur:str) -> str:
    article_text = "Fake article content for now"
    return article_text

def summarize_text(text:str) ->str:
    summary = "Fake summary of the article content for now"
    return summary 